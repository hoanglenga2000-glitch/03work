# WPS 表格自动调整工具使用说明

## 📋 功能说明

本工具可以自动调整 WPS 文档中所有表格的宽度，使其根据窗口自动调整：

- ✅ **自动调整宽度**：所有表格自动调整列宽以适应窗口

## 🚀 使用方法

### 方法一：使用 Python 脚本（推荐）

#### 步骤 1：安装依赖

打开命令提示符（CMD）或 PowerShell，运行：

```bash
pip install pywin32
```

#### 步骤 2：运行脚本

**方式 A：双击运行批处理文件**
- 双击 `wps_table_formatter.bat` 文件
- 脚本会自动检查环境并运行

**方式 B：直接运行 Python 脚本**
```bash
python wps_table_formatter.py
```

#### 步骤 3：确保 WPS 已打开

- ✅ WPS 必须已经运行
- ✅ 要处理的文档必须在 WPS 中打开
- ✅ 文档中必须包含至少一个表格

### 方法二：使用 VBScript 脚本（无需安装 Python）

1. 确保 WPS 已打开，并且要处理的文档已打开
2. 双击运行 `WPS表格格式化宏.vbs` 文件
3. 等待处理完成，会弹出提示框

## ⚠️ 注意事项

1. **WPS 版本要求**
   - 需要 WPS Office 专业版或企业版（支持 COM 接口）
   - 个人版可能不支持 COM 接口

2. **文档状态**
   - 文档必须在 WPS 中打开
   - 建议先保存文档，以防意外

3. **表格要求**
   - 文档中必须包含至少一个表格
   - 如果文档中没有表格，脚本会提示并退出

4. **权限问题**
   - 如果遇到权限错误，请以管理员身份运行脚本

## 🔧 故障排除

### 问题 1：提示"无法连接到 WPS"

**解决方案：**
- 确保 WPS 已经打开
- 确保至少有一个文档在 WPS 中打开
- 检查 WPS 版本是否支持 COM 接口

### 问题 2：提示"未安装 pywin32"

**解决方案：**
```bash
pip install pywin32
```

如果 pip 命令不可用，请先安装 Python 3.x

### 问题 3：脚本运行但没有效果

**可能原因：**
- 文档中没有表格
- WPS 版本不支持某些功能
- 文档被其他程序锁定

**解决方案：**
- 检查文档中是否确实有表格
- 尝试手动保存文档后再运行脚本
- 关闭其他可能占用文档的程序

### 问题 4：表格没有自动调整宽度

**解决方案：**
- 这是 WPS COM 接口的限制
- 可以手动在 WPS 中选择表格 → 右键 → 自动调整 → 根据窗口调整表格

## 📝 脚本说明

### Python 脚本 (`wps_table_formatter.py`)

- **功能完整**：支持所有格式化需求
- **错误处理**：包含详细的错误提示
- **进度显示**：显示处理进度

### VBScript 脚本 (`WPS表格格式化宏.vbs`)

- **无需安装**：Windows 系统自带支持
- **简单易用**：双击即可运行
- **功能相同**：实现相同的格式化功能

### 批处理文件 (`wps_table_formatter.bat`)

- **自动检查**：自动检查 Python 和依赖库
- **自动安装**：自动安装缺失的依赖
- **用户友好**：提供清晰的提示信息

## 💡 使用建议

1. **批量处理**：可以打开多个文档，逐个运行脚本处理
2. **备份文档**：处理前建议先备份原文档
3. **检查结果**：处理完成后检查表格格式是否符合要求
4. **手动调整**：如果自动调整不理想，可以手动微调

## 🎯 示例

处理前：
```
标题

[表格]（文字左对齐，有多余空行）

[表格]（列宽不合适）
```

处理后：
```
标题
[表格]（文字居中，无多余空行）
[表格]（自动调整列宽）
```

## 📞 技术支持

如果遇到问题，请检查：
1. WPS 版本和文档状态
2. Python 环境（如果使用 Python 脚本）
3. 错误提示信息

---

**祝使用愉快！** 🎉
